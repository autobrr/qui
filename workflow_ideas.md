# Ideas for workflow branch (feat/tracker-rules-delete-mode)

- Global workflows, or a way to duplicate/copy workflows into same instance or other instances
- Multiple actions per workflow
- Regex validation in the UI (UI is TS, backend is Go - worth noting)